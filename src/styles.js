const app = {
  textAlign: 'center',
  height: '100vh',
  display: 'flex',
  flexDirection: 'column'
}

const pageBackground = {
  height: '100%',
  fontSize: 'calc(10px + 2vmin)',
  color: 'white'
}

const appLink = {
  color: '#61dafb'
}

const styles = {
  app,
  pageBackground,
  appLink
}

export default styles
